return {
  "akinsho/toggleterm.nvim",
  opts = {
    size = 50, -- Fixed small height so it doesn't "grow" visually
    persist_size = false, -- No saving weird sizes
    close_on_exit = "toggle", -- Ensures clean close on toggle
    direction = "vertical",
  },
}
